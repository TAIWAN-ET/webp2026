# CS_smoke

A small React smoke-test app scaffolded in Create React App style.

## Scripts

- `npm start`
- `npm test`
- `npm run build`

## Notes

Install dependencies with `npm install` before starting the app.
